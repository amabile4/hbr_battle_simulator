# フェーズ3 移植ロジック検証レポート

本ドキュメントは、Python版ダメージ計算エンジン（`damage_calc_engine.py`）から JS/TS（`hbr_battle_simulator`）への移植作業（フェーズ3）に伴い実施した、計算式・ロジックの検証結果報告書です。

---

## 1. 検証結果サマリー

実施した検証の範囲内において、JS/TS移植エンジンが Python版および Excelモデルと同一の計算ロジックで動作していることを確認しました。

| 検証項目 | 対象件数 | 合格件数 | 不一致件数 | 結果 | 備考 |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **① 固定境界ケース検証 (Fixed Fixtures)** | 6 ケース | 6 ケース | 0 ケース | **SUCCESS** | 主要計算ロジック（バフ・デバフ乗算限界等）の検証 |
| **② Excel格子点突合回帰テスト (Regression)** | 115 行 | 115 行 | 0 ケース | **SUCCESS** | Excel固有のクランプやダミー行を吸収する互換レイヤー併用 |
| **③ 大規模ランダム差分テスト (Differential)** | 2,000 ケース | 2,000 ケース | 0 ケース | **SUCCESS** | ランダムパラメータにおけるPython版との差分検証 |

---

## 2. 検証再現性および実行環境

検証用テストコードおよびテストデータは、テスト実行環境の依存性（Excel解析モジュールや大規模データ容量など）を分離するため、`hbr_calc` リポジトリ側で一元管理されています。

### A. 前提条件と環境構築
検証を実行するには、`hbr_calc` リポジトリ内に `hbr_battle_simulator` へのシンボリックリンクを作成する必要があります。
```bash
# hbr_calc ディレクトリにて実行
ln -s ../hbr_battle_simulator/json json
ln -s ../hbr_battle_simulator/src src
```

### B. 各検証の実行手順とファイル所在

#### ① 固定境界ケース検証 (Fixed Fixtures)
- **概要**: 基礎ダメージ、バフ枠キャップ、デバフ上限分類、脆弱弱点判定、クリティカル心眼枠、連撃トークン乗算など、仕様書で定義された主要な境界値における数式モデルの検証。
- **使用スクリプト**: `hbr_calc/run_js_fixed_fixtures_tests.mjs`
- **入力データ**: `hbr_calc/test_cases_fixed.json` (6ケース)
- **実行コマンド**:
  ```bash
  node run_js_fixed_fixtures_tests.mjs
  ```

#### ② Excel格子点突合回帰テスト (Regression)
- **概要**: Excel（`HBR計算機🎭Ver.4.31.03.xlsx`）の有効な115行から抽出したパラメータと計算結果（期待通常・クリティカルダメージ等）との差分検証（許容誤差 `1e-4` 以下）。
- **使用スクリプト**: `hbr_calc/run_js_regression_tests.py` （内部で `run_js_calc_helper.mjs` を呼び出し）
- **入力データ**: `hbr_calc/HBR計算機🎭Ver.4.31.03.xlsx`
- **実行コマンド**:
  ```bash
  uv run python run_js_regression_tests.py
  ```
  *(注: macOS環境での実行権限の問題を避けるため、`uv run python` 経由での実行を推奨)*

#### ③ 大規模ランダム差分テスト (Differential) & カバレッジ測定
- **概要**: 力・器用さなどのステータス（300〜1000）、レベル、限界突破、ランダムバフ・デバフ、戦闘ゾーンなどを網羅した2,000件のランダムケースにおける、Python版とJS版の出力の同一性検証。
- **使用スクリプト**: `hbr_calc/run_js_large_tests.mjs`
- **入力データ**: `hbr_calc/test_cases_large.json`（`generate_test_cases_large.py` により生成）
- **実行コマンド**:
  ```bash
  node --experimental-test-coverage --test run_js_large_tests.mjs
  ```

### C. 実行ログの保存先
検証実行時の標準出力およびカバレッジ結果の生ログは、以下にエビデンスとして格納されています。
- **ログファイル**: [docs/calc/test_execution_log.txt](file:///Users/ram4/git/hbr_battle_simulator/docs/calc/test_execution_log.txt)

---

## 3. コードカバー率（C0 カバレッジ）検証結果

大規模ランダム差分テスト（2,000件）の実行と同時に測定した、コアファイル `damage-calculator.js` のコードカバー率は以下の通りです。

- **行網率 (Line Coverage)**: **84.22%**
- **カバレッジの評価**:
  - ダメージ計算の主要な数式処理や分岐（能動的な計算パス）はほぼ全域が網羅されています。
  - 未カバーの約15.78%は、テストデータが正常（実マスタが整合している状態）な場合には通過しない例外系（データ欠損時のエラーハンドリングなど）や、次に述べる category fallback warning 用の名称ベース自動判定処理に留まるため、検証の網羅性としては十分であると判断できます。

---

## 4. 仕様変更と Excelモデルとの乖離要因

回帰テストにおいて突合を行うため、検証用スクリプト上で「Excel互換レイヤー（データモックの注入など）」を適用しています。これは、JS/TS移植版において実データ駆動への改善や仕様の適正化を行ったことに起因する正常な乖離です。

### 1) 非攻撃スキルのダメージクランプ (0.0クランプ)
- **内容**: `ソフニング` などのデバフ専用スキルについて、ExcelおよびPython版（Excel模倣時）では数式コピーの都合上ダメージが算出されていましたが、JS版では「攻撃Partを持たないスキルは基礎ダメージを0にする」という仕様が正しく適用されています。

### 2) 条件分岐スキル（OD有無・使用回数による分岐）の扱い
- **内容**: `石塔の手筋+` などのスキルについて、Excelでは「通常時」「OD時」等の条件ごとに別行として表現されていますが、JS版ではマスタの構造に則って状態の静的解決（第1要素の処理）を正しく行っています。

### 3) 手動SP / 閾値調整の適正化
- **内容**: 一部スキルにおいて、Excelモデルでは消費SPなどを手動で書き換えた上で計算していましたが、JS版は `skills.json` 内の実ゲームデータに準拠して計算を行うため、より実際のゲーム内容に則した値を出力するよう改善されています。

---

## 5. Excelエクスポート後の関数破損・参照ズレへの自動補完方針

スプレッドシートから Excel (.xlsx) / OpenDocument (.ods) 形式へ変換したファイルをテストや解析に使用する際、変換エンジンに起因する関数つぶれ（`__xludf.DUMMYFUNCTION`）や結合セル・配列数式（`ArrayFormula`）の競合による参照先セルズレが発生し、シート内の自動判定やバフ値計算が不活性化します。
これらに対して、テスト側で正しく計算・検証を行うための補完方針は以下の通りです。

### 1) 自動判定破損（DUMMYFUNCTION化）への補完
- **現象**: 5,527箇所におよぶ自動反映判定（`REGEXMATCH` 関数を使った脆弱や侵食の検出など）が Excel 変換によってすべて不活性化し、評価値が `None`/空白になります。
- **補完方針**:
  - テストランナー（`run_js_regression_tests.py` 等）において、これらの自動抽出に頼らず、テストケース側で明示的にバフ・デバフの構造化オブジェクト（`statusEffects`）を構成して注入します。
  - プログラム上での恒久対策としては、Googleスプレッドシート側で `REGEXMATCH` を `ISNUMBER(SEARCH("パターン", セル))` に置き換えて数式を再構成することで、Excel/LibreOffice 双方での完全互換を確保できます。

### 2) フィールドバフ等の参照先セルズレの補完
- **現象**: 結合セルのマッピング崩れ等により、期待値 `Y16` 等の計算式において、正しいフィールド合計セル `AS32` ではなく、定数 `0.0` の `AQ53` が参照されています。
- **補完方針**:
  - テスト実行時には、Excel側の期待値と JS 電卓の出力を突合するため、JS側の入力にフィールドバフ（Zone）は含めず（またはExcel側の参照先である定数 `0.0` に合わせて 0 として入力し）、計算ロジック自体の検証を優先します。
  - もし Excel ファイルの修復を行う場合は、期待値テーブル（Y列・AB列）の乗算から `$AQ$53+100%` を削除し、攻撃バフ合計に正しくフィールド `AS32` を加算した数式 `(AJ67 + AS32 + 100%)` に再配線します。

### 3) 脆弱（AJ94）の未参照バグの補完
- **現象**: 脆弱合計 `AJ94` が、デバフ合計を計算している `BI262` には加算されていますが、期待値テーブル（Y列/AB列）および詳細計算の逆算元（AS262/AS263）の双方において参照されていません。
- **補完方針**:
  - テストコード側では、Excel 側の期待値に合わせるため、入力としての `debuff` には脆弱効果を注入せず、単体の `DefenseDown` のみを入力してテストを走らせます。
  - Excel側を修復する際は、Y列/AB列に `+AJ94` をデバフ枠として加算し、`AS262`/`AS263` のデバフ逆算部分を `($BI$262+100%)` で割る形に修復します。

---

## 6. Category Fallback Warning の扱いについて（T3.4.1）

`damage-calculator.js` の一部処理には、`category` が未定義の場合にスキル名からカテゴリ（`DefenseDown` など）を推定するフォールバックロジックが存在しています。
- **計算結果への影響**: 本検証における 2,000 件のテストケースおよび Excel 突合検証において、このフォールバックによる計算結果の狂いは発生しておらず、**非ブロッカー（計算の正当性に影響を与えないもの）**であることを確認しています。
- **今後の対応方針**: このフォールバックは、既存 fixture との互換性維持のために残されています。より厳密な設計（strict contract）へ移行して名称ベースのフォールバックを完全に排除するかどうかについては、フェーズ4（UI・画面連携）の進捗を見ながらフォローアップタスクとして検討を継続します。

---

## 7. 結論

本検証により、`hbr_battle_simulator` に移植された `damage-calculator.js` のダメージ計算ロジックは、設計仕様書に定義された計算モデルを正確に再現しており、実ゲームデータベース（`skills.json` 等）と整合する形で適切に動作していることが確認されました。

これをもって、フェーズ3（移植ロジックの検証）の実装および検証作業を完了とします。
