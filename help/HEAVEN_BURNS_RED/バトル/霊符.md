# ヘルプ：HEAVEN BURNS RED > バトル > 霊符

## 基本情報

- **URL**: <https://wfs-heaven-burns-red.zendesk.com/hc/ja/articles/50970892519321-%E9%9C%8A%E7%AC%A6>
- **カテゴリ**: HEAVEN BURNS RED > バトル

## ヘルプ記載内容

霊符
バトル中に付与されるデバフ効果の1つです。霊符レベルに応じて全能力が減少します。霊符状態で攻撃を受けたときに霊符レベルが+1されます。※Hit数に関わらず1回の攻撃で霊符レベルが+1されます。※敵の行動終了時にレベルは0にリセットされます※霊符状態を重複させることはできません※全能力を下げる効果を重ねがけした場合、ダメージ算出において効果値の高いほうで算出されます

> **主語の補足（紛らわしい箇所の明確化）**: 霊符状態は**敵に**付与されるデバフです。「攻撃を受けたとき」の主語も**敵**（霊符状態になった敵）であり、プレイヤーパーティーが敵を攻撃するたびに霊符レベルが上昇します。「敵の行動終了時」のリセットも同様に敵ターンを指します。

## シミュレーター実装情報

- **内部名**: `Talisman`

### 詳細仕様 (解析情報)

#### 貼ったりましょう！ (スキル等により付与)

- バトル開始時 敵に霊符状態(ターン永続/解除不可)を付与する

#### 全体 霊符 (Talisman)

- 戦闘開始時、**敵に**レベル0の霊符デバフを付与する。
- 霊符レベルが1上がるごとに、**敵の**全ステータスが10減少する（最大レベル: 10）。
- **霊符状態の敵が**プレイヤーの攻撃を受けるごとに、霊符レベルが1上昇する（1攻撃=+1、Hit数不問）。
- **敵の**ターン終了時にレベルは0にリセットされる。
- ※ 同種の効果とは重複しない。
- ※ 効果値が高いものが優先される。
- ※ 解除不可（Eternal）。

### 実装メモ

パッシブスキル（例：ようこそ♪ナイトメア・パレードの國見タマ）により戦闘開始時に付与される特殊なデバフ状態として実装します。「攻撃を受けるたびにデバフLvがダイナミックに上昇し、敵ターン終了時にLvがリセットされる」という独自のサイクルを持つため、専用の `Talisman` 状態を敵側のステータスとして保持管理する機構が必要です。被弾回数に応じてレベルを上げ、デバフ計算時にはこの現在のレベル数（最大10） × 10 のステータス減少量を適用します。永続かつ解除不可（Eternal/Cannot be removed）のデバフとして扱います。

### 実装調査メモ（2026-03-09）

#### データ構造

`skills.json` / `passives.json` での `skill_type` は `Talisman`。`power[0]` の値で動作が分岐する。

| `power[0]` | `value[0]` | 動作 |
|-----------|-----------|------|
| `0` | `0` | **初期付与**: 霊符状態を active=true、level=0 で敵に付与する |
| `2`（正の値） | `1` | **レベル増加**: `value[0]=1` は「霊符状態のときのみ」条件。レベルを +N する（最大10） |

#### スキルデータの実例

- `貼ったりましょう！`（skills.json, id: 46401601）: `is_restricted=1`, `passive.timing=OnBattleStart`, `parts[0]={skill_type:Talisman, power:[0,0], value:[0,0]}`
  - → バトル開始時に敵へ霊符を初期付与するトリガースキルパッシブ
- `恐怖の叫び`（passives.json → styles.json `passives[]`）: `timing=OnFirstBattleStart`, `parts=[{AdditionalHitOnExtraSkill}, {Talisman, power:[2,0], value:[1,0]}]`
  - → EXスキル使用後（`AdditionalHitOnExtraSkill` トリガー条件）に霊符レベル+2
- `ハロウィンナイト`（passives.json → styles.json `passives[]`）: `timing=OnPlayerTurnStart`, 条件「霊符状態かつ氷属性スタイル4体以上」、`parts=[{Talisman, power:[2,0], value:[1,0]}]`
  - → ターン開始時に条件満たせば霊符レベル+2

#### 現在のシミュレーター実装状態（2026-03-09時点）

| 機能 | 状態 |
|------|------|
| `enemyState.talismanState` の追加（`active`, `level`, `maxLevel`） | ✅ 実装済み |
| 初期付与パッシブ（`power[0]=0`）の発火 | ✅ 実装済み |
| レベル増加パッシブ（`power[0]>0`、`value[0]=1` 条件チェック） | ✅ 実装済み |
| 被弾時の自動レベル増加（プレイヤーのアクション処理との統合） | ✅ 実装済み |
| 敵ターン終了時のレベル自動リセット | ✅ 実装済み |
| `IsTalisman` 条件式の評価（`ハロウィンナイト` 等） | ✅ 実装済み |
| ダメージ計算への霊符レベル反映（全ステータス -10×level） | ❌ 未実装 |

### 所持スタイルリスト (霊符 での検索結果)

#### スキル (skills.json)

- **國見 タマ** [ようこそ♪ナイトメア・パレード]
  - 貼ったりましょう！
  - バトル開始時 敵に霊符状態(ターン永続/解除不可)を付与する

#### パッシブ (passives.json)

- **國見 タマ** [ようこそ♪ナイトメア・パレード]
  - 恐怖の叫び
  - 敵が霊符状態のとき 自身がEXスキル使用後 敵の霊符レベル+2(最大レベル10)
- **國見 タマ** [ようこそ♪ナイトメア・パレード]
  - ハロウィンナイト
  - ターン開始時 敵が霊符状態かつ氷属性スタイルが 4体以上のとき 敵の霊符レベル+2(最大レベル10)
